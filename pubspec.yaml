name: wearable_app
description: "Smart Wearable Monitor PI5"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.34.0
  permission_handler: ^11.3.1
  url_launcher: ^6.3.0
  google_fonts: ^6.2.1
  http: ^1.2.0
  flutter_tts: ^4.2.0        # Síntese de voz nativa
  audioplayers: ^6.0.0       # Sinal sonoro/sirene
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true